# Web Playground 🚀

HTML basics and Git workflow practice repository.

## 📋 Overview
- **Sample.html**: HTML concept like headings
- Git version control practice
- Commit conventions best practices

## ✅ Tasks Completed
- Repository initialized with `.gitignore`
- User configured (`user.name`, `user.email`)
- Default branch set to `main`
- Commands practiced: `git log`, `git diff`, `git restore`, `git status`, `git add`, `git commit`

## � Commit Convention

Following **[Conventional Commits](https://www.conventionalcommits.org/)** specification.

**Format:** `<type>(<scope>): <subject>`

**Types:** `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`

**Examples:**
```bash
feat(html): add form validation
docs(readme): update documentation
chore(git): add .gitignore
```
